

// services/Users.js
const createUser(name, age) => {
  user = User({ userId, userId });
  user.save();
}


// services/Payments.js
const makePayment(id, userId) => {
  payment = Payment({ userId });
  payment.save();
}
